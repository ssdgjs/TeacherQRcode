# ====================================
# EduQR AI - 环境变量配置示例
# ====================================
# 复制此文件为 .env 并修改相应配置

# ==================== 数据库配置 ====================
# PostgreSQL 数据库连接（必填）
# 格式: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://eduqr:eduqr_password@localhost:5432/eduqr

# ==================== 认证配置 ====================
# JWT 密钥（必填）- 用于签名和验证 JWT token
# 生产环境必须使用强随机字符串，至少32位
JWT_SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars

# ==================== 管理员配置 ====================
# 管理员暗号（必填）- 用于创建二维码的密码
ADMIN_PASSWORD=Avic2026!

# ==================== 应用配置 ====================
# 服务器基础 URL（必填）- 用于生成活码完整链接
# 本地测试: http://localhost:8000
# 生产环境: http://your-ip 或 http://your-domain.com
BASE_URL=http://localhost:8000

# 数据保留天数（可选，默认 30 天）
# 超过此天数的作业记录和音频文件将被自动清理
DATA_RETENTION_DAYS=30

# 最大内容长度（可选，默认 10000 字符）
MAX_CONTENT_LENGTH=10000

# 最大上传文件大小（可选，默认 20MB）
MAX_UPLOAD_SIZE_MB=20

# 允许的音频文件扩展名（可选，默认：mp3,wav,m4a,ogg）
ALLOWED_AUDIO_EXTENSIONS=mp3,wav,m4a,ogg

# 默认二维码尺寸（可选，默认 300px）
QR_CODE_SIZE=300

# 默认容错率（可选，默认：M）
# L=7%, M=15%, Q=25%, H=30%
QR_ERROR_CORRECTION=M

# ==================== 额度管理配置 ====================
# 每日免费次数上限（可选，默认 10）
FREE_DAILY_LIMIT=10

# 100次价格（可选，默认 990分 = 9.9元）
PACKAGE_PRICE_100=990

# 月卡价格（可选，默认 990分 = 9.9元）
MONTHLY_PRICE=990

# ==================== AI 服务配置（v0.3.0+）====================
# 智谱 AI API 密钥
ZHIPU_API_KEY=your-zhipu-api-key

# ==================== TTS 服务配置（v0.4.0+）====================
# 火山引擎 Access Key
VOLCENGINE_ACCESS_KEY=your-volcengine-access-key
VOLCENGINE_SECRET_KEY=your-volcengine-secret-key
